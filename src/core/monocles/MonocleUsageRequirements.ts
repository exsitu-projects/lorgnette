export interface MonocleProviderUsageRequirements {
    /**
     * List of the IDs of the languages that are supported by the monocle.
     * Not specifying the parameter is equivalent to specifying all languages.
     */
     languages?: string[];
}