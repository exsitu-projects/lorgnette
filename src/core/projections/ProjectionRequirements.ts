export interface ProjectionRequirements {
    /**
     * List of the IDs of the languages that are supported by the projection.
     * Not specifying the parameter is equivalent to specifying all languages.
     */
     languages?: string[];
}